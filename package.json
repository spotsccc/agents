{
  "name": "agents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:all": "vp run check -r",
    "ready": "vp run check -r && vp run test -r && vp run build -r",
    "dev": "vp run website#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.32.1"
}
